# src/features/projects/ — Agent Rules

Projects feature module.

- Project creation, selection, and configuration
- `projectsUtils.ts` — project data helpers
- Use "Project" not "workspace" or "site"

See `/AGENTS.md` for project-wide rules.
