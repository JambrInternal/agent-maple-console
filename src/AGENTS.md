# src/ — Agent Rules

Application source root. Mixed TypeScript/JSX codebase.

- Services and API layer: TypeScript (`.ts`)
- Pages and UI components: JSX (`.jsx`) or TSX (`.tsx`)
- Entry point: `main.tsx` → `App.tsx` → `app/AppProviders.tsx` + `app/AppRoutes.tsx`

See `/AGENTS.md` for project-wide rules.
