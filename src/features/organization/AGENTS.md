# src/features/organization/ — Agent Rules

Organization feature module.

- Organization selection and management
- `orgSelectionUtils.ts` — org picker logic
- Use "Organization" not "tenant" or "company"

See `/AGENTS.md` for project-wide rules.
