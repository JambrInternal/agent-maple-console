# src/contexts/ — Agent Rules

React context providers.

- `AuthContext.tsx` — provides auth state (user, tokens, login/logout) to the component tree
- Keep contexts minimal — business logic belongs in `services/`, not here

See `/AGENTS.md` for project-wide rules.
