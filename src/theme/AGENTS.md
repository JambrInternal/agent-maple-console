# src/theme — Agent Rules

Theme token source files for shared visual primitives.

- Put reusable CSS variables here (colors, typography, spacing, radius, motion, semantic/component state tokens).
- Keep token names prefixed with `--am-`.
- Keep this directory focused on tokens and theme-layer styles, not component-specific layout rules.

See /AGENTS.md for project-wide rules.
