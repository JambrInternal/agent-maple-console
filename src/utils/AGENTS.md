# src/utils/ — Agent Rules

Shared utilities.

- `verboseLogger.js` — use this instead of `console.log` (the only file exempt from `no-console`)
- Utilities here must be generic — feature-specific helpers go in `features/<name>/`

See `/AGENTS.md` for project-wide rules.
