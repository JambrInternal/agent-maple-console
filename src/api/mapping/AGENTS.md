# src/api/mapping/ — Agent Rules

API response mappers.

- Transform raw API responses into UI-friendly models
- Keep mappers pure — no side effects, no API calls

See `src/api/AGENTS.md` for API-layer rules. See `/AGENTS.md` for project-wide rules.
