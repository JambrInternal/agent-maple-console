# src/hooks/ — Agent Rules

Shared custom React hooks.

- `useApiQuery.js` — TanStack Query wrapper for API calls
- Hooks here must be generic/reusable — feature-specific hooks go in `features/<name>/`

See `/AGENTS.md` for project-wide rules.
