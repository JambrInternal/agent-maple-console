# src/components/ui — Agent Rules

Shared design-system UI primitives (shadcn-style wrappers).

- Keep components generic and composable (`Button`, `Input`, `Textarea`, etc.).
- Drive styles through design tokens (`--am-*`) rather than hardcoded colors.
- Keep variant APIs stable and explicit (`variant`, `size`).

See /AGENTS.md for project-wide rules.
